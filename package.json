{
  "name": "eficienta",
  "version": "0.1.0",
  "private": true,
  "homepage": ".",
  "dependencies": {
    "react": "^18.1.0",
    "react-dom": "^18.1.0",
    "react-multi-date-picker": "^3.3.0",
    "react-scripts": "^5.0.1",
    "react-to-print": "^2.14.7"
  },
  "scripts": {
    "start": "set PORT=5000 set HOST=0.0.0.0 && react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
